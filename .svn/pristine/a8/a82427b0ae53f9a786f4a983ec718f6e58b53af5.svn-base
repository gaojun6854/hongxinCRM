package com.hongxin.service;

import java.util.List;
import com.hongxin.entity.CustomStatus;

public interface CustomStatusService 
{
	CustomStatus load(Integer id);

	CustomStatus get(Integer id);

	List<CustomStatus> findAll();

	void persist(CustomStatus entity);

	Integer save(CustomStatus entity);

	void saveOrUpdate(CustomStatus entity);

	void delete(Integer id);

	void flush();

	String save4StrId(CustomStatus customStatus);
	
	CustomStatus getByStrId(String id);
}
